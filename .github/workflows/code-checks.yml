name: Code Checks

on:
  push:
    branches:
      - main
      - 2.3.x
  pull_request:
    branches:
      - main
      - 2.3.x

jobs:
  code-checks:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -el {0}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          auto-activate-base: true
          activate-environment: ""  # Don't activate any environment yet
          environment-file: environment.yml

      - name: Create Conda Environment
        run: |
          echo "Creating environment from environment.yml..."
          conda env create -f environment.yml
          source /usr/share/miniconda/etc/profile.d/conda.sh  # Ensure Conda is available
          conda activate Project-Korora  # Activate the environment

      - name: Install pre-commit
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh  # Ensure Conda is available
          conda activate Project-Korora  # Activate environment again
          pip install pre-commit

      - name: Verify pre-commit installation
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh  # Ensure Conda is available
          conda activate Project-Korora  # Activate environment
          echo "Installed conda packages:"
          conda list
          echo "Pre-commit executable location:"
          which pre-commit

      - name: Run Pre-commit Hooks (Linting & Formatting)
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh  # Ensure Conda is available
          conda activate Project-Korora  # Activate environment
          pre-commit run --all-files

      - name: Run Tests
        run: |
          source /usr/share/miniconda/etc/profile.d/conda.sh  # Ensure Conda is available
          conda activate Project-Korora  # Activate environment
          pytest
